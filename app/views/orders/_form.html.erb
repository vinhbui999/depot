<%= form_for @order do |form| %>
  <% if @order.errors.any? %>
    <div id="error_explanation">
      <h2><%=raw t('errors.template.header', count: @order.errors.count,
      model: t('activerecord.models.order'))%></h2>
      <ul>
        <% @order.errors.full_messages.each do |error| %>
          <li><%=raw error %></li>
        <% end %>
      </ul>
    </div>
  <% end %>
  <div class="field">
    <%= form.label :name , t('.name')%>
    <%= form.text_field :name, value: @index%>
  </div>
  <%= form.hidden_field :user_id%>
  <div class="field">
    <%= form.label :address , t('.address')%>
    <%= form.text_area :address, rows: 3, cols: 40 %>
  </div>
  <div class="field">
    <%= form.label :email, t('.email') %>
    <%= form.text_field :email, size: 40 %>
  </div>
  <div class="field">
    <%= form.label :pay_type , t('.paytype')%>
    <%= form.select :pay_type, t(Order.pay_types.keys.map{|i| '.'.concat(i)}), prompt: t('.selectpay') %>
  </div>
  <div class="field">
    <%= form.label :ship_date, t('.shipdate')%>
    <%= form.text_field :ship_date %>
  </div>
  <div class="actions">
    <%= form.submit t('.placeorder')%>
  </div>
<% end %>
